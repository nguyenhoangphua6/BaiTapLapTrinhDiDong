<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.midterm.lamnhom">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.LamNhom"
        android:usesCleartextTraffic="true">
        <activity
            android:label="Thông tin về covid"
            android:name=".model.NamKhongActivity"
            android:exported="true" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:label="Khai báo y tế"
            android:name=".thongtinthongbao"
            android:exported="true"></activity>
        <activity
            android:label="Kết quả truy vết"
            android:name=".KQTimKiem"
            android:exported="true" />
        <activity
            android:label="Truy vết"
            android:name=".SearchActivity"
            android:exported="true" />
    </application>

</manifest>